name: Keep Supabase awake

on:
  schedule:
    - cron: "0 3 */3 * *" # every 3 days at 03:00 UTC
  workflow_dispatch: {}

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping health endpoint
        run: |
          curl -fsS "${{ secrets.APP_URL }}/api/health" || exit 1
